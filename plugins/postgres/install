#!/bin/bash

# Build docker image
git clone https://github.com/brianpattison/dokku-pg-dockerfiles /tmp/dokku-pg-dockerfiles
docker build -q=true -t brianpattison/postgres /tmp/dokku-pg-dockerfiles
rm -rf /tmp/dokku-pg-dockerfiles

# Install postgres-client
apt-get install -y postgres-client
