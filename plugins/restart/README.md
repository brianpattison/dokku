## [Dokku](https://github.com/progrium/dokku) plugin to restart an app without a git push

Commands
--------
```
$ dokku help
     restart <app>     Restarts specified app
     restart:all       Restarts all apps
```
